"""anna-web — placeholder package for the Phase 2.5 web dashboard.

The dashboard implementation lands in a separate plan; this package
exists today only to reserve the ``anna-web`` console-script entry so
the dashboard work doesn't need to touch pyproject.toml or trigger a
reinstall when it lands.
"""
